# Desarrollo local: dejar vacío — el proxy de Vite (vite.config.ts) redirige al backend.
# Staging / producción: apuntar a la URL real del backend (sin barra final).
# Recordar también configurar CORS_ORIGINS en el .env del backend con el origen del frontend.
VITE_API_URL=
